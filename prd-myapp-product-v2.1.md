# MyAPP 产品需求文档

| 字段 | 内容 |
|---|---|
| 文档版本 | v3.0 |
| 日期 | 2026-03-16 |
| 项目状态 | 个人项目 |
| 技术栈 | React + TypeScript + Capacitor |

---

## 一、项目背景

MyAPP 是一个个人开发基于已有的开源项目二创的 AI-Native 应用，目前是 Web 端，移动端尚在开发中。

整体形态是一个模拟手机 OS 的界面（"小手机"），里面内置了聊天、相册、工作区文件浏览器等模块，核心是和一个固定的 AI 角色进行交互。这个 AI 角色的名字、人设、头像都由用户自己定义，叫什么取决于使用者的配置，项目本身就叫 MyAPP。

**目前阶段**：核心聊天功能已基本实现，文件/媒体/语音/电话/表情包等能力已完成初版，记忆系统和主动性机制仍在开发中。

---

## 二、技术架构简述

- **前端**：React + TypeScript，运行在 Capacitor 壳（Web + 原生桥接）
- **存储**：IndexedDB（热缓存）+ 本地 Markdown 文件（真相源）+ Backup DB（快照库）
- **LLM**：SSE 直连外部 API（DeepSeek / OpenAI / 火山 / MiniMax，可切换）
- **打包目标**：Web 开发态 + Android APK

---

## 三、项目定位

一个高度定制化的 AI 交互界面，核心特点是：

1. **单角色、深度定制**：整个应用只有一个 AI 角色，角色的名字、人设、头像完全由用户自定义，不做角色市场。
2. **与本地文件深度集成**：AI 可以读写用户配置的本地工作区目录（实体文件夹），聊天内容、记忆、媒体文件都落在本地。
3. **持久记忆**：对话记录跨会话保留，并有定期归档压缩机制，AI 的记忆会随时间积累。
4. **一定的主动性**：AI 不只是被动回答，有心跳调度机制，可以在合适的时机主动发消息。
5. **拟人化交互**：聊天节奏、语音交互、电话模式都尽量模拟真实的人与人沟通体验，而不是问答框那种形式。

---

## 四、现有功能（已完成）

记录当前已实现的能力，作为后续开发的基线。

### 3.1 核心聊天
- 文字聊天，SSE 流式接收 AI 回复
- 拟人化消息节奏：AI 回复多条消息时按字数动态延迟，逐条发出，带"正在输入"动画
- 消息防抖合并：用户连续发送多条消息时，等待一段时间打包后统一处理（时长可配置）
- 引用回复：AI 回复多个独立问题时，每段回复有对应的引用指示块，点击可跳转原消息
- 消息多选删除
- 链接预览：发送链接时前端展示预览卡片
- 联网搜索（Tavily API）

### 3.2 媒体与文件
- 图片发送：用户发送图片存入相册，含去重逻辑
- 视频发送：存入相册，预览显示封面 + 播放图标，支持点击播放
- 文件双向传输：用户和 AI 均可发送/接收文件（txt/md/pdf/doc/docx/音频等），文件按类型存入工作区 `temp/` 子目录
- 工作区文件浏览器：支持 CRUD 及文件/文件夹移动

### 3.3 表情包 & Emoji
- 表情包双向发送，支持静态图和 GIF
- AI 可"读懂"表情包：扫描 `stickers/` 目录建立索引，通过 RAG 检索相关表情包名称（约 15 个）匹配上下文
- 表情包记忆格式：`[[SEND_EMOJI: 名称]]`，DB 只存名称，渲染层实时映射路径
- RAG 优化：分类索引 + 使用频率/最近使用排序权重
- 原生 Emoji 符号支持（AI 可发送/识别）

### 3.4 语音消息
- 用户按住说话发送语音消息
- 文字/语音输入切换
- AI 发送语音消息（条件触发，含语音能量系统控制频率）
- 连续语音对话模式：用户发语音 → AI 默认语音回复，2 条纯文字后自动降级
- 语音意图锁：用户明确说"发文字"后，锁定文字模式直至解锁
- 语音消息转写文本预存，一键查看不需要重复播放
- Reroll 重新生成，旧语音文件自动清理

### 3.5 电话功能
- 基础双向语音通话
- 打字机效果同步文字显示
- AI 接通后第一句话根据上下文决定
- 拒接回复根据上下文动态生成（不固定话术）
- 兜底文字显示

### 3.6 首次启动 & 角色配置
- 首次启动引导：检测工作区/相册目录是否已配置，引导填写或自动生成
- 角色灵魂文件（`Agent_Soul.md`）：检测是否存在，不存在则引导用户填写角色名称、人设、头像
- AI 优化人设：用户输入初稿后可调用模型优化文本（需先配置 API）
- 角色名称完全由用户自定义（如 Nova、Mori 等），与项目本身名称无关
- 用户档案（`User_Profile.md`）：首次启动引导填写，支持后续在配置区编辑，双向同步含冲突锁
- 角色头像更换（用户发起 / AI 主动提议）

### 3.7 数据治理基础
- IndexedDB 为热缓存，物理文件夹为唯一数据源（Shadow Workspace）
- 数据库版本迁移机制（版本号只增不减，迁移幂等化）
- 启动流水线：版本校验 → 迁移 → 验证 → 恢复降级
- 分环境数据库命名隔离（dev / staging / prod）
- 全局数据 ZIP 备份与挂载入口

---

## 五、待开发功能

以下是规划中但尚未完成的功能，按优先级排列。

---

### P0 — 必须先做

#### 4.1 记忆系统完善

**背景**：目前 AI 有基础聊天记录，但没有系统化的记忆归档机制，长对话后 Context 会越来越长、越来越贵，且 AI 容易"忘事"。

**每日记忆归档（CRON）**
- 每天定时（默认 23:00）对当天对话执行摘要归档，写入工作区 Markdown 文件
- 归档后历史对话不删除，但调用时用摘要替代原始内容，控制 Context 长度

**双数据库备份**
- 独立 Backup DB 存关键数据快照（聊天记录、用户档案、角色配置等）
- 自动快照触发条件：每 50 条消息 / 每 5 分钟 / 进入后台前 / 敏感操作前
- 启动失败时自动从最近快照恢复；用户可在"恢复中心"手动选时间点恢复

**恢复中心 UI**
- 设置页显示最近备份时间
- 提供"恢复上次备份"入口
- 出现启动异常时进入安全模式，显示友好提示而非直接黑屏

---

### P1 — 核心体验补全

#### 4.2 Prompt 模块化重构（Skill 化）

**背景**：当前 System Prompt 是一个大段文字，所有能力都混在一起。随着功能增多，AI 在长对话中越来越容易忽略某些规则，且每次调用的 Token 浪费较多。

**方向**：将文件系统、联网搜索、表情包、语音等能力拆分为独立模块（Skill），根据当前场景/意图按需注入，而不是每次都全量载入。

**待决策**：
- 第一期拆分哪些模块？（建议：文件系统、联网搜索、语音控制三个）
- 是否在 UI 上展示"当前已加载的技能"？

---

#### 4.3 心跳主动消息

**背景**：目前 AI 完全被动，只有用户先开口才会回应。

**需要做的事**：
- 后台定期唤醒（每 30-60 分钟），检查是否有值得主动发消息的情况
- 触发条件参考：当前时间（深夜/清晨特殊时段）、用户近期提到的事项（约定、计划等）、长时间没有对话
- 触发后以普通气泡形式推送，不做系统通知

**约束**：
- 相邻两次主动消息间隔至少 30 分钟
- 用户可配置"免打扰等级"（关闭 / 正常 / 活跃）
- 深夜时段（23:00-8:00）降低触发频率

---

#### 4.4 电话：用户先发声

**背景**：目前电话接通后 AI 总是先说话，但用户作为主叫方应该可以先开口。

**需要做的事**：
- 电话接通后，等待约 1 秒——用户先说话则 AI 倾听；用户无声则 AI 先开口
- 用户说话期间，等用户停顿（约 1-1.5 秒静音）后再触发 AI 回应
- 用户连说几句时，类似聊天区多条消息合并，打包后统一回应

**后续待定**：AI 说话途中用户打断，AI 中断并切换为倾听。技术复杂度较高，暂不排期，先记录。

---

### P2 — 体验完善

#### 4.5 工作区权限边界

**背景**：目前仅靠 Prompt 约束 AI 的文件读写范围，不够可靠。

**需要做的事**：
- 前端执行器对 AI 的文件操作请求进行白名单校验，越界访问直接拦截，不依赖 Prompt
- 用户可在设置中查看"AI 当前可访问的目录"

---


#### 4.6 记忆金字塔（五层 待优化决策）

目前记忆归档是平铺的，长期来看需要分层压缩，防止积累的记忆本身也变得过长。

**设计方向**：

```
近期对话（原始）
  ↓ 每日归档
日摘要（关键事件、情绪）
  ↓ 月度压缩
月摘要（重要人物、约定、转折）
  ↓ 长期沉淀
用户背景认知（身份、习惯、重要关系）
  ↓
核心价值观 & 性格判断
```

语义检索基于本地向量数据库（Voy 或类似 Wasm 方案），不出设备。

---

### P3 — 未来方向（暂不排期）

- **电话真正意义上的打断**：AI 说话时用户开口，AI 中断
- **悬浮球 + 屏幕感知**：申请系统权限，在其他 App 上层展示悬浮球，可截屏发给 AI 分析
- **本地 TTS 推理**（Fish Speech）：替代云端语音服务


---

## 七、待决策的问题

| # | 问题 | 涉及模块 |
|---|---|---|
| 1 | 记忆归档内容是否要对用户可见？（比如提供"AI 记住了什么"的查看页面） | 记忆系统 |
| 2 | 数据 ZIP 导出是否包含语音缓存文件？（会影响包大小） | 数据治理 |
| 3 | 启动异常的安全模式提示，用技术语言还是角色语言？ | 异常体验 |
| 4 | Skill 模块化第一期拆哪几个能力？ | Prompt 架构 |
| 5 | 电话等待用户先说话的静音阈值是否做成可配置参数？ | 电话 |
| 6 | 用户拒绝 AI 主动提议（如换头像）后冷却期多久？拒绝几次后停止提议？ | 主动性 |

---

## 五、进度总览

| 模块 | 状态 |
|---|---|
| 基础 OS 框架 + 单主角 | ✅ 完成 |
| SSE 直连 + 拟人化聊天 | ✅ 完成 |
| 文件 / 媒体 / 表情包 | ✅ 完成 |
| 语音消息 + 基础电话 | ✅ 完成 |
| 首次启动引导 + 角色设置 | ✅ 完成 |
| 基础记忆归档（CRON + Shadow Workspace） | ✅ 完成 |
| 数据自动备份 / 安全恢复 | 🔴 进行中（P0） |
| 五层记忆 RAG | 🟡 规划（P1） |
| Skill 插件化 / Prompt 模块化 | 🟡 规划（P1） |
| 心跳主动消息（完善版） | 🟡 规划（P1） |
| 电话支持用户先发声 | 🟡 待优化（P1） |
| 工作区权限强化 | 🟡 待优化（P2） |
| Agent 驱动代码生成（L4） | 🟡 待优化（P2） |
| 悬浮球 + 视觉捕获（L3） | 🟢 未来方向（P3） |